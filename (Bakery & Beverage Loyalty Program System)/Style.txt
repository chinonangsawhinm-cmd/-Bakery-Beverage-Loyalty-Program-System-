<style>
:root{
  --brown:#4a2c1a; --caramel:#b5651d; --gold:#e8a33d;
  --cream:#fdf6ec; --soft:#f7e7d3; --grey:#7a6a5d; --green:#2e7d52;
}
*{margin:0;padding:0;box-sizing:border-box}
body{font-family:'Prompt',sans-serif;background:var(--cream);color:var(--brown);line-height:1.65}
a{text-decoration:none;color:inherit;cursor:pointer}
.container{max-width:1150px;margin:0 auto;padding:0 18px}
.bg-soft{background:var(--soft)}
.loading{grid-column:1/-1;text-align:center;padding:50px;color:var(--grey)}

header{position:sticky;top:0;z-index:900;background:rgba(74,44,26,.97)}
nav{display:flex;align-items:center;justify-content:space-between;padding:12px 0;color:#fff}
.brand{display:flex;align-items:center;gap:11px}
.brand-mark{width:44px;height:44px;border-radius:14px;background:var(--gold);color:var(--brown);display:grid;place-items:center;font-size:22px}
.brand b{font-size:18px;display:block;line-height:1.1;letter-spacing:.5px}
.brand span{font-size:10.5px;opacity:.7;letter-spacing:2px}
.nav-right{display:flex;align-items:center;gap:10px}
.nav-link{font-size:14.5px;padding:8px 12px;border-radius:20px;transition:.2s;cursor:pointer}
.nav-link:hover{background:rgba(255,255,255,.12)}
.cart-btn{position:relative;background:var(--gold);color:var(--brown);padding:9px 18px;border-radius:22px;font-weight:500;border:0;cursor:pointer;font-family:inherit;font-size:14px}
.badge{position:absolute;top:-6px;right:-6px;background:#d64545;color:#fff;font-size:11px;min-width:20px;height:20px;border-radius:50%;display:grid;place-items:center;font-weight:600}
.user-chip{display:flex;align-items:center;gap:8px;background:rgba(255,255,255,.13);padding:6px 14px 6px 6px;border-radius:22px;cursor:pointer;font-size:13.5px}
.avatar{width:30px;height:30px;border-radius:50%;background:var(--gold);color:var(--brown);display:grid;place-items:center;font-weight:600;font-size:13px}

.btn{font-family:inherit;font-size:15px;padding:12px 28px;border-radius:30px;border:0;cursor:pointer;font-weight:500;transition:.22s}
.btn-primary{background:var(--caramel);color:#fff}
.btn-primary:hover{background:#9c5316;transform:translateY(-2px)}
.btn-gold{background:var(--gold);color:var(--brown)}
.btn-gold:hover{transform:translateY(-2px);box-shadow:0 8px 20px rgba(232,163,61,.4)}
.btn-ghost{background:transparent;border:2px solid var(--caramel);color:var(--caramel)}
.btn-ghost:hover{background:var(--caramel);color:#fff}
.hero-ghost{border-color:#fff;color:#fff;margin-left:8px}
.btn-block{width:100%;display:block}
.btn:disabled{opacity:.45;cursor:not-allowed;transform:none!important}

.hero{min-height:70vh;display:grid;place-items:center;text-align:center;color:#fff;
 background:linear-gradient(rgba(74,44,26,.72),rgba(74,44,26,.85)),url('https://images.unsplash.com/photo-1509440159596-0249088772ff?w=1600') center/cover}
.hero h1{font-size:clamp(30px,5.5vw,54px);font-weight:700;margin-bottom:8px}
.hero .tag{color:var(--gold);letter-spacing:4px;font-size:13px;margin-bottom:14px}
.hero p{max-width:560px;margin:0 auto 26px;opacity:.93;font-weight:300}

section{padding:64px 0}
.sec-title{text-align:center;margin-bottom:38px}
.sec-title h2{font-size:29px;position:relative;display:inline-block;padding-bottom:12px}
.sec-title h2::after{content:'';position:absolute;left:50%;bottom:0;transform:translateX(-50%);width:60px;height:4px;background:var(--gold);border-radius:3px}
.sec-title p{color:var(--grey);margin-top:12px;font-size:14.5px}

.tabs{display:flex;justify-content:center;gap:10px;flex-wrap:wrap;margin-bottom:32px}
.tab{padding:10px 24px;border-radius:26px;background:#fff;border:2px solid var(--soft);cursor:pointer;font-family:inherit;font-size:14.5px;color:var(--grey);transition:.2s}
.tab.active{background:var(--brown);border-color:var(--brown);color:#fff}

.menu-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(240px,1fr));gap:22px}
.item{background:#fff;border-radius:18px;overflow:hidden;box-shadow:0 4px 16px rgba(74,44,26,.08);transition:.26s;display:flex;flex-direction:column}
.item:hover{transform:translateY(-7px);box-shadow:0 16px 34px rgba(74,44,26,.16)}
.item-img{height:165px;background-size:cover;background-position:center;position:relative}
.hot{position:absolute;top:10px;left:10px;background:#d64545;color:#fff;font-size:11px;padding:4px 11px;border-radius:12px;font-weight:500}
.item-body{padding:16px;flex:1;display:flex;flex-direction:column}
.item-body h4{font-size:16.5px;margin-bottom:4px}
.item-body .desc{font-size:12.5px;color:var(--grey);flex:1;margin-bottom:12px}
.item-foot{display:flex;align-items:center;justify-content:space-between}
.price{font-size:19px;font-weight:600;color:var(--caramel)}
.add{background:var(--brown);color:#fff;border:0;width:38px;height:38px;border-radius:12px;font-size:20px;cursor:pointer;transition:.2s}
.add:hover{background:var(--caramel);transform:rotate(90deg)}
.feat{padding:26px;text-align:center}
.feat .fi{font-size:38px}
.feat h4{margin:10px 0 6px}

.points-box{background:linear-gradient(135deg,var(--brown),#6b4226);color:#fff;border-radius:22px;padding:34px;display:grid;grid-template-columns:1fr 1.3fr;gap:34px;align-items:center}
.pt-left{text-align:center}
.pt-cap{font-size:13px;opacity:.75}
.pt-num{font-size:52px;font-weight:700;color:var(--gold);line-height:1}
.pt-bar{height:11px;background:rgba(255,255,255,.2);border-radius:8px;overflow:hidden;margin:14px 0 8px}
.pt-fill{height:100%;width:0;background:var(--gold);border-radius:8px;transition:.5s}
.pt-hint{font-size:12px;opacity:.72}
.rw-title{font-size:19px;margin-bottom:14px}
.rewards{display:grid;gap:10px}
.reward{background:rgba(255,255,255,.1);border-radius:14px;padding:13px 16px;display:flex;align-items:center;justify-content:space-between;gap:12px}
.reward.ok{background:rgba(232,163,61,.24);border:1px solid var(--gold)}
.reward small{display:block;opacity:.72;font-size:11.5px}
.rd-btn{background:var(--gold);color:var(--brown);border:0;padding:7px 16px;border-radius:16px;font-family:inherit;font-size:12.5px;cursor:pointer;font-weight:500;white-space:nowrap}
.rd-btn:disabled{background:rgba(255,255,255,.16);color:rgba(255,255,255,.5);cursor:not-allowed}

.overlay{position:fixed;inset:0;background:rgba(40,22,10,.62);z-index:1000;display:grid;place-items:center;padding:18px;overflow-y:auto}
.modal{background:#fff;border-radius:22px;width:100%;max-width:440px;padding:32px;animation:pop .28s ease;max-height:92vh;overflow-y:auto}
.modal.wide{max-width:560px}
@keyframes pop{from{opacity:0;transform:scale(.93) translateY(14px)}to{opacity:1;transform:none}}
.modal h3{font-size:22px;margin-bottom:6px}
.modal .sub{color:var(--grey);font-size:13.5px;margin-bottom:22px}
.close{float:right;background:none;border:0;font-size:26px;color:var(--grey);cursor:pointer;line-height:1}
.field{margin-bottom:15px}
.field label{display:block;font-size:13.5px;margin-bottom:6px;font-weight:500}
.field input{width:100%;padding:12px 15px;border:2px solid var(--soft);border-radius:12px;font-family:inherit;font-size:14.5px;background:var(--cream);transition:.2s}
.field input:focus{outline:0;border-color:var(--gold);background:#fff}
.switch{text-align:center;font-size:13.5px;color:var(--grey);margin-top:16px}
.switch b{color:var(--caramel);cursor:pointer}
.err{background:#fdecec;color:#b53434;font-size:13px;padding:10px 14px;border-radius:10px;margin-bottom:14px}

.drawer{position:fixed;top:0;right:0;width:400px;max-width:100%;height:100%;background:#fff;z-index:1001;box-shadow:-8px 0 40px rgba(0,0,0,.2);display:flex;flex-direction:column;animation:slide .3s ease}
@keyframes slide{from{transform:translateX(100%)}to{transform:none}}
.drawer-head{padding:22px;border-bottom:2px solid var(--soft);display:flex;justify-content:space-between;align-items:center}
.drawer-head h3{font-size:20px}
.drawer-body{flex:1;overflow-y:auto;padding:18px 22px}
.drawer-foot{padding:22px;border-top:2px solid var(--soft);background:var(--cream)}
.line{display:flex;gap:13px;padding:13px 0;border-bottom:1px solid var(--soft)}
.line-img{width:62px;height:62px;border-radius:12px;background-size:cover;background-position:center;flex-shrink:0}
.line-info{flex:1}
.line-info b{font-size:14.5px;display:block}
.line-info small{color:var(--grey);font-size:12.5px}
.line-sum{font-weight:600;color:var(--caramel)}
.qty{display:flex;align-items:center;gap:9px;margin-top:7px}
.qty button{width:26px;height:26px;border-radius:8px;border:2px solid var(--soft);background:#fff;cursor:pointer;font-size:15px;color:var(--brown);line-height:1}
.qty button:hover{border-color:var(--caramel);color:var(--caramel)}
.qty span{min-width:22px;text-align:center;font-weight:500}
.sum-row{display:flex;justify-content:space-between;font-size:14.5px;margin-bottom:8px}
.sum-row.total{font-size:19px;font-weight:600;padding-top:11px;border-top:2px dashed var(--soft)}
.sum-row.total span:last-child{color:var(--caramel)}
.sum-row.disc{color:var(--green);font-weight:500}
.empty{text-align:center;padding:56px 0;color:var(--grey)}
.empty div{font-size:52px;margin-bottom:12px;opacity:.35}

.pay-wrap{text-align:center}
.qr-card{background:#fff;border:3px solid var(--soft);border-radius:20px;padding:22px;display:inline-block;margin:6px 0 16px}
.qr-card img{display:block;width:230px;height:230px}
.promptpay{background:#003d7a;color:#fff;font-size:12px;letter-spacing:2px;padding:7px 0;border-radius:8px 8px 0 0;margin:-22px -22px 16px;font-weight:600}
.pay-amt{font-size:36px;font-weight:700;color:var(--caramel)}
.timer{display:inline-block;background:var(--soft);padding:6px 18px;border-radius:20px;font-size:14px;margin:12px 0 20px}
.timer b{color:#d64545}
.order-ref{font-size:13px;color:var(--grey);margin-bottom:4px}
.note{font-size:11.5px;color:var(--grey);margin-top:14px}
.tick{width:88px;height:88px;border-radius:50%;background:var(--green);color:#fff;font-size:46px;display:grid;place-items:center;margin:0 auto 18px;animation:pop .4s ease}
.gain{background:var(--soft);border-radius:14px;padding:15px;margin:18px 0;font-size:14.5px}
.gain b{color:var(--caramel);font-size:19px}
.gain small{display:block;font-size:13px;color:var(--grey);margin-top:4px}

.hist{border:2px solid var(--soft);border-radius:14px;padding:15px;margin-bottom:12px;text-align:left}
.hist-top{display:flex;justify-content:space-between;font-size:13px;color:var(--grey);margin-bottom:6px}
.hist b{font-size:15px}
.hist p{font-size:12.5px;color:var(--grey)}
.status{background:#e3f5ea;color:var(--green);font-size:11.5px;padding:3px 11px;border-radius:12px}
.status.pending{background:#fff3e0;color:#c47b00}

.spin{width:34px;height:34px;border:4px solid var(--soft);border-top-color:var(--caramel);border-radius:50%;margin:26px auto;animation:sp .8s linear infinite}
@keyframes sp{to{transform:rotate(360deg)}}

footer{background:var(--brown);color:#e6d5c4;padding:44px 0 28px;text-align:center;font-size:13.5px}
.f-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:26px;text-align:left;margin-bottom:26px}
.f-grid h4{color:#fff;font-size:15.5px;margin-bottom:10px}
.f-grid p{opacity:.78;font-size:13px;margin-bottom:5px}
.copy{border-top:1px solid rgba(255,255,255,.14);padding-top:18px;opacity:.65;font-size:12.5px}

@media(max-width:860px){
  .points-box{grid-template-columns:1fr;padding:26px}
  .f-grid{grid-template-columns:1fr;text-align:center}
  .nav-link{display:none}
}
</style>